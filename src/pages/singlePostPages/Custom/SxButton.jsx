

import { makeStyles } from "@mui/styles";

export const SxButton = makeStyles({
  root: {
    textTransform: "none",
    display: "flex",
    flexDirection: "column",
    borderRadius: "15px",
    padding: 40,
    marginTop: 50 },
});
